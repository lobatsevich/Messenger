services:
    db:
        image: postgres:16-alpine
        container_name: messenger_db
        restart: unless-stopped
        environment:
            POSTGRES_DB: ${POSTGRES_DB:-messenger}
            POSTGRES_USER: ${POSTGRES_USER:-messenger}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-messenger}
        ports:
            - "5432:5432"
        volumes:
            - postgres_data:/var/lib/postgresql/data
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
            interval: 5s
            timeout: 5s
            retries: 20

    app:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: messenger_app
        restart: unless-stopped
        env_file:
            - .env
        environment:
            DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER:-messenger}:${POSTGRES_PASSWORD:-messenger}@db:5432/${POSTGRES_DB:-messenger}
            SECRET_KEY: ${SECRET_KEY:-change-me-in-env}
        depends_on:
            db:
                condition: service_healthy
        ports:
            - "8000:8000"

volumes:
    postgres_data:
